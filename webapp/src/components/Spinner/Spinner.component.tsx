import React from 'react';

import {Spinner as MMSpinner} from '@brightscout/mattermost-ui-library';
export const Spinner = () => {
    return (
        <div>
            <MMSpinner/>
        </div>
    );
};

